{
  "name": "agentops",
  "version": "0.1.0",
  "private": true,
  "description": "The Control Plane for Autonomous Agent Teams",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @agentops/core && npm run build -w @agentops/db && npm run build -w @agentops/sdk && npm run build -w @agentops/web && npm run build -w @agentops/cli",
    "test": "npm run test --workspaces --if-present",
    "dev": "npm run dev --workspace=packages/web"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
